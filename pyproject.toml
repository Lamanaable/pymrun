[project]
name = "pymrun"
version = "0.1.0"
description = "Fuzzy Python module runner for uv"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["click>=8.0"]

[project.scripts]
pymrun = "pymrun.cli:main"

[dependency-groups]
dev = ["pytest"]
